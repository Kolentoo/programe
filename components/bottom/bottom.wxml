<!--components/bottom/bottom.wxml-->
<view class="bottom">
  <text bindtap="tabchange" data-item="{{item.active}}" data-type="{{index}}" class="bottom-list {{item.active===true?'bottom-on':''}}" wx:for="{{bottomdata}}" wx:key="this">{{item.name}}</text>
</view>
